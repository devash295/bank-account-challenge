export enum TransactionType {
    WITHDRAW = 'Withdraw',
    DEPOSIT = 'Deposit',
    TRANSFER = 'Transfer',
}

export enum TransactionStatus {
    PENDING = 'Pending',
    COMPLETED = 'Completed',
    CANCELLED = 'Cancelled',
}